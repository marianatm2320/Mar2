<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Ghost</title>
    <style>
        canvas { text-align:center; align-items: center;}
    </style>
  </head>  
  <body>
    <canvas id="canvas" width="1500" height="650"></canvas>
    
    <script src="utils.js"></script>
    <script>
    window.onload = function () {
      var canvas = document.getElementById('canvas'),
          context = canvas.getContext('2d'),
          angleX = 0,
          angleY = 0,
          range = 100,
          centerX = 600;
          centerY = 150,
          xspeed = 1,
          xspeedneg=-1, 
          yspeed = 0.02;
          xpos= 0; 
          ypos= 0; 

            luc = new Image();
            luc.src="luciernaga.png";

      context.lineWidth = 2;

      (function drawFrame () {
        window.requestAnimationFrame(drawFrame, canvas);
        context.clearRect(0, 0, innerWidth, innerHeight);

        luc.x = centerX + Math.sin(angleX) * range;
        luc.y = centerY + Math.sin(angleY) * range;
        angleX += xspeed;
        angleY += yspeed;
        
        console.log(xpos);
        xpos += xspeed;
        if(xpos>1500 || xpos<0){
       xspeed = -xspeed; 
      } 
        ypos = centerY + Math.sin(angleY) * range;
        context.drawImage(luc, xpos, ypos, 300, 200);
        context.restore();

      }());
    };
    </script>
  </body>
</html>